"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var initShader_1 = require("../initShader");
var initShaderProgram = function (gl, vsSource, fsSource) {
    var vsShader = (0, initShader_1.default)(gl, gl.VERTEX_SHADER, vsSource);
    var fsShader = (0, initShader_1.default)(gl, gl.FRAGMENT_SHADER, fsSource);
    var program = gl.createProgram();
    if (program) {
        if (vsShader && fsShader) {
            gl.attachShader(program, vsShader);
            gl.attachShader(program, fsShader);
            gl.linkProgram(program);
            if (!gl.getProgramParameter(program, gl.LINK_STATUS)) {
                console.error('Unable to initialize the shader program: ' +
                    gl.getProgramInfoLog(program));
                return null;
            }
            return program;
        }
    }
};
exports.default = initShaderProgram;
//# sourceMappingURL=index.js.map